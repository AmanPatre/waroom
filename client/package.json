{
  "name": "warroom-client",
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "spacetimedb": "^2.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.1",
    "vite": "^8.0.3"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "generate-bindings": "spacetime generate --module-path .. --lang typescript --out-dir src/module_bindings || \"%LOCALAPPDATA%\\SpacetimeDB\\spacetime.exe\" generate --module-path .. --lang typescript --out-dir src/module_bindings"
  }
}